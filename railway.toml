[build]
builder = "nixpacks"
buildCommand = "go build -ldflags=-w -s -o out ./cmd/server"

[deploy]
startCommand = "./out"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
